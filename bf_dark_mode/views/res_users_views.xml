<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Full backend user form (admins): allows setting a default for others -->
    <record id="view_users_form_bf_dark_mode" model="ir.ui.view">
        <field name="name">res.users.form.bf.dark.mode</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <field name="tz" position="after">
                <field name="bf_dark_mode_enabled"/>
            </field>
        </field>
    </record>

    <!-- Preferences dialog (each user editing their own profile) -->
    <record id="view_users_form_simple_modif_bf_dark_mode" model="ir.ui.view">
        <field name="name">res.users.preferences.bf.dark.mode</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form_simple_modif"/>
        <field name="arch" type="xml">
            <field name="tz" position="after">
                <field name="bf_dark_mode_enabled"/>
            </field>
        </field>
    </record>

</odoo>
