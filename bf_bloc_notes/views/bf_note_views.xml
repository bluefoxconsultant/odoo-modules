<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="bf_note_view_form" model="ir.ui.view">
        <field name="name">bf.note.form</field>
        <field name="model">bf.note</field>
        <field name="arch" type="xml">
            <form string="Note rapide">
                <header>
                    <button name="action_create_activity_quick"
                            type="object"
                            string="🗓️ Aujourd'hui"
                            class="btn-primary"
                            context="{'offset': 0}"/>
                    <button name="action_create_activity_quick"
                            type="object"
                            string="Demain"
                            context="{'offset': 1}"/>
                    <button name="action_create_activity_quick"
                            type="object"
                            string="+2 jours"
                            context="{'offset': 2}"/>
                    <button name="action_create_activity_quick"
                            type="object"
                            string="+1 semaine"
                            context="{'offset': 7}"/>
                    <button name="action_open_activity_wizard"
                            type="object"
                            string="Personnaliser…"/>
                    <button name="action_open_task_wizard"
                            type="object"
                            string="Convertir en tâche"
                            class="btn-secondary"/>
                    <button name="action_open_reroute_wizard"
                            type="object"
                            string="Re-router…"
                            class="btn-secondary"/>
                    <button name="action_toggle_pinned"
                            type="object"
                            string="Épingler"
                            invisible="pinned"/>
                    <button name="action_toggle_pinned"
                            type="object"
                            string="Désépingler"
                            invisible="not pinned"/>
                    <button name="toggle_active"
                            type="object"
                            string="Archiver"
                            class="btn-secondary"
                            invisible="not active"
                            confirm="Archiver cette note ?"/>
                    <button name="toggle_active"
                            type="object"
                            string="Désarchiver"
                            class="btn-secondary"
                            invisible="active"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_open_record"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-external-link"
                                invisible="not res_id">
                            <field name="res_name" string="Voir la fiche"/>
                        </button>
                        <button name="action_open_activities"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-tasks"
                                invisible="tracked_activity_count == 0">
                            <field name="tracked_activity_count" widget="statinfo" string="Activités"/>
                        </button>
                        <button name="action_open_tasks"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-check-square-o"
                                invisible="tracked_task_count == 0">
                            <field name="tracked_task_count" widget="statinfo" string="Tâches"/>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Archivée"
                            bg_color="text-bg-danger"
                            invisible="active"/>
                    <widget name="web_ribbon" title="Épinglée"
                            bg_color="text-bg-warning"
                            invisible="not pinned or not active"/>
                    <widget name="web_ribbon" title="Partagée"
                            bg_color="text-bg-info"
                            invisible="not is_shared or not active or pinned"/>
                    <field name="active" invisible="1"/>
                    <field name="color" widget="color_picker"/>
                    <div class="oe_title">
                        <label for="name" class="oe_edit_only"/>
                        <h1><field name="name" placeholder="Titre (auto si vide)"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="user_id" options="{'no_create': True}"/>
                            <field name="tag_ids" widget="many2many_tags"
                                   options="{'color_field': 'color', 'no_create_edit': True}"/>
                            <field name="deadline_date"/>
                        </group>
                        <group>
                            <field name="is_shared"/>
                            <field name="pinned"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Contenu" name="body">
                            <field name="body"
                                   widget="html"
                                   options="{'collaborative': false}"
                                   placeholder="Écris ta note ici…"/>
                        </page>
                        <page string="Liens" name="links">
                            <field name="link_ids">
                                <list editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="target_ref" string="Fiche"/>
                                    <field name="res_name" string="Nom"/>
                                    <button name="action_open" type="object" icon="fa-external-link" string="Ouvrir"/>
                                </list>
                            </field>
                            <p class="text-muted small">
                                Astuce : « Re-router… » dans l'entête déplace la note vers une autre fiche
                                (ou l'ajoute à une seconde) à partir d'une URL, d'un numéro ou d'une référence.
                            </p>
                            <group>
                                <field name="res_ref" string="Ajouter un lien primaire"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="bf_note_view_list" model="ir.ui.view">
        <field name="name">bf.note.list</field>
        <field name="model">bf.note</field>
        <field name="arch" type="xml">
            <list string="Notes" decoration-warning="pinned"
                  decoration-danger="deadline_date and deadline_date &lt; current_date"
                  multi_edit="1">
                <field name="pinned" widget="boolean_toggle"/>
                <field name="is_shared" widget="boolean_toggle" optional="hide"/>
                <field name="name"/>
                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                <field name="res_name" string="Lié à"/>
                <field name="deadline_date" optional="show"/>
                <field name="user_id" widget="many2one_avatar_user"/>
                <field name="write_date" string="Modifiée"/>
            </list>
        </field>
    </record>

    <record id="bf_note_view_kanban" model="ir.ui.view">
        <field name="name">bf.note.kanban</field>
        <field name="model">bf.note</field>
        <field name="arch" type="xml">
            <kanban default_order="pinned desc, write_date desc"
                    class="o_kanban_dashboard o_bf_note_kanban"
                    sample="1">
                <field name="color"/>
                <field name="pinned"/>
                <field name="is_shared"/>
                <field name="active"/>
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="oe_kanban_card oe_kanban_global_click o_bf_note_card #{kanban_color(record.color.raw_value)}">
                            <div class="o_dropdown_kanban dropdown">
                                <a role="button" class="dropdown-toggle btn px-1 py-0"
                                   data-bs-toggle="dropdown" data-bs-display="static"
                                   aria-label="Actions" title="Actions">
                                    <span class="fa fa-ellipsis-v"/>
                                </a>
                                <div class="dropdown-menu" role="menu">
                                    <a role="menuitem" type="edit" class="dropdown-item"
                                       t-if="widget.editable">Modifier</a>
                                    <a role="menuitem" type="object" name="toggle_active"
                                       class="dropdown-item">
                                        <t t-if="record.active.raw_value">Archiver</t>
                                        <t t-else="">Désarchiver</t>
                                    </a>
                                    <a role="menuitem" type="delete" class="dropdown-item"
                                       t-if="widget.deletable">Supprimer</a>
                                    <div role="separator" class="dropdown-divider"/>
                                    <ul class="oe_kanban_colorpicker" data-field="color"/>
                                </div>
                            </div>
                            <div class="o_bf_note_card_color_strip"/>
                            <div class="o_bf_note_card_header d-flex align-items-start">
                                <strong class="flex-grow-1">
                                    <field name="name"/>
                                </strong>
                                <button class="btn btn-link p-0 ms-1 o_bf_note_pin_btn"
                                        type="object"
                                        name="action_toggle_pinned"
                                        title="Épingler / désépingler"
                                        t-attf-style="opacity: #{record.pinned.raw_value ? 1 : 0.35};">
                                    <i class="fa fa-thumb-tack"/>
                                </button>
                                <i t-if="record.is_shared.raw_value"
                                   class="fa fa-users ms-1 text-info"
                                   title="Partagée"/>
                            </div>
                            <div class="o_bf_note_card_body">
                                <field name="body" widget="html_text" class="text-muted"/>
                            </div>
                            <div class="o_bf_note_card_footer d-flex align-items-center mt-2">
                                <field name="tag_ids"
                                       widget="many2many_tags"
                                       options="{'color_field': 'color'}"
                                       class="flex-grow-1"/>
                                <field name="user_id" widget="many2one_avatar_user"/>
                            </div>
                            <div t-if="record.res_name.raw_value" class="text-muted small mt-1">
                                <i class="fa fa-link" title="Lié à"/> <field name="res_name"/>
                            </div>
                            <div t-if="record.deadline_date.raw_value"
                                 t-attf-class="small #{record.deadline_date.raw_value &lt; (new Date()).toISOString().slice(0,10) ? 'text-danger fw-bold' : 'text-muted'}">
                                <i class="fa fa-calendar" title="Échéance"/> <field name="deadline_date"/>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="bf_note_view_calendar" model="ir.ui.view">
        <field name="name">bf.note.calendar</field>
        <field name="model">bf.note</field>
        <field name="arch" type="xml">
            <calendar string="Notes"
                      date_start="deadline_date"
                      color="color"
                      mode="month"
                      quick_create="0">
                <field name="name"/>
                <field name="tag_ids"/>
                <field name="user_id"/>
                <field name="res_name"/>
            </calendar>
        </field>
    </record>

    <record id="bf_note_view_search" model="ir.ui.view">
        <field name="name">bf.note.search</field>
        <field name="model">bf.note</field>
        <field name="arch" type="xml">
            <search string="Notes">
                <field name="name"/>
                <field name="body"/>
                <field name="tag_ids"/>
                <field name="res_name"/>
                <filter name="my_notes" string="Mes notes" domain="[('user_id', '=', uid)]"/>
                <filter name="pinned" string="Épinglées" domain="[('pinned', '=', True)]"/>
                <filter name="shared" string="Partagées" domain="[('is_shared', '=', True)]"/>
                <filter name="linked" string="Liées à une fiche" domain="[('link_ids', '!=', False)]"/>
                <separator/>
                <filter name="filter_today" string="Modifiées aujourd'hui"
                        domain="[('write_date', '&gt;=', context_today().strftime('%Y-%m-%d'))]"/>
                <filter name="filter_week" string="Cette semaine"
                        domain="[('write_date', '&gt;=', (context_today() - relativedelta(days=7)).strftime('%Y-%m-%d'))]"/>
                <separator/>
                <filter name="deadline_overdue" string="Échéance dépassée"
                        domain="[('deadline_date', '&lt;', context_today().strftime('%Y-%m-%d'))]"/>
                <filter name="deadline_today" string="Échéance aujourd'hui"
                        domain="[('deadline_date', '=', context_today().strftime('%Y-%m-%d'))]"/>
                <separator/>
                <filter name="archived" string="Archivées" domain="[('active', '=', False)]"/>
                <group expand="0" string="Regrouper par">
                    <filter name="group_user" string="Auteur" context="{'group_by': 'user_id'}"/>
                    <filter name="group_tag" string="Étiquette" context="{'group_by': 'tag_ids'}"/>
                    <filter name="group_color" string="Couleur" context="{'group_by': 'color'}"/>
                    <filter name="group_model" string="Modèle lié" context="{'group_by': 'res_model'}"/>
                    <filter name="group_deadline" string="Échéance" context="{'group_by': 'deadline_date'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="bf_note_action" model="ir.actions.act_window">
        <field name="name">Bloc-notes</field>
        <field name="res_model">bf.note</field>
        <field name="view_mode">kanban,list,calendar,form</field>
        <field name="search_view_id" ref="bf_note_view_search"/>
        <field name="context">{'search_default_my_notes': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Crée ta première note rapide
            </p>
            <p>
                Raccourci : <b>Alt+N</b> pour une nouvelle note, <b>Alt+Shift+N</b> pour la liste.
            </p>
        </field>
    </record>

    <record id="bf_note_action_pinned" model="ir.actions.act_window">
        <field name="name">Notes épinglées</field>
        <field name="res_model">bf.note</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="domain">[('pinned', '=', True), '|', ('user_id', '=', uid), ('is_shared', '=', True)]</field>
        <field name="context">{'search_default_pinned': 1}</field>
    </record>

</odoo>
