<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_contact_persona_category_list" model="ir.ui.view">
        <field name="name">contact.persona.category.list</field>
        <field name="model">contact.persona.category</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="code"/>
                <field name="color" widget="color_picker"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <record id="action_contact_persona_category" model="ir.actions.act_window">
        <field name="name">Catégories de communication</field>
        <field name="res_model">contact.persona.category</field>
        <field name="view_mode">list</field>
    </record>

</odoo>
