<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Action planifiée : Vérifier l'expiration des identifiants -->
        <record id="ir_cron_check_expiring_credentials" model="ir.cron">
            <field name="name">Identifiants : Vérifier les expirations</field>
            <field name="model_id" ref="model_project_credential"/>
            <field name="state">code</field>
            <field name="code">model._cron_check_expiring_credentials()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active">True</field>
        </record>

    </data>
</odoo>
