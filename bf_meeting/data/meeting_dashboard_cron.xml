<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Disabled : the meeting digest is now embedded in
             `daily_todo_digest` ("🌄 Votre journée"). Kept as a callable
             method for ad-hoc triggers via `model._cron_send_daily_digest()`. -->
        <record id="cron_meeting_dashboard_daily_digest" model="ir.cron">
            <field name="name">Rencontres : digest quotidien (legacy, désactivé)</field>
            <field name="model_id" ref="model_meeting_dashboard"/>
            <field name="state">code</field>
            <field name="code">model._cron_send_daily_digest()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="False"/>
            <field name="nextcall" eval="(datetime.utcnow().replace(hour=11, minute=0, second=0, microsecond=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
        </record>

    </data>
</odoo>
