<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="res_partner_view_form_inherit_bf_email" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.bf.email</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_bf_emails"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-envelope"
                        invisible="bf_email_count == 0">
                    <field name="bf_email_count" widget="statinfo" string="Courriels"/>
                </button>
            </xpath>
        </field>
    </record>

</odoo>
