<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Contacts list ▸ Actions ▸ Enrichir depuis les signatures courriel.
         Queues the selected contacts for the background cron. Group-gated. -->
    <record id="action_bf_batch_enrich_signatures" model="ir.actions.server">
        <field name="name">Enrichir depuis les signatures courriel</field>
        <field name="model_id" ref="base.model_res_partner"/>
        <field name="binding_model_id" ref="base.model_res_partner"/>
        <field name="binding_view_types">list</field>
        <field name="groups_id" eval="[(4, ref('bf_contact_enrichment.group_bf_contact_enrich'))]"/>
        <field name="state">code</field>
        <field name="code">action = records.action_bf_queue_signature_enrich()</field>
    </record>

</odoo>
